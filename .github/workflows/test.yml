name: Build and Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out the code
      uses: actions/checkout@v2

    - name: Build
      run: |
        echo "Building the project..."
    
    - name: Test
      run: |
        echo "Running tests..."
